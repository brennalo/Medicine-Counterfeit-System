BLOCKCHAIN_PRIVATE_KEY=ac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80 


DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=pharmachain_username
DB_PASSWORD=12345
DB_NAME=pharmachain_bcd

JWT_SECRET=blockchain-development-assignment


// 0x prefix not included, corresponds to the #0 account generated by Hardhat's local node
// This file is used by the frontend to connect to the local blockchain and access deployed contracts
// the private key is used for signing transactions from the frontend (e.g. when registering a user or adding a medicine)
// In a production environment, you would never hardcode private keys like this. Instead, you would use a secure vault or environment variable management system.
// For development purposes, this allows the frontend to interact with the local blockchain and deployed contracts without needing a separate wallet setup. The private key corresponds to the first account generated by Hardhat's local node, which is used for deploying contracts and performing transactions during development.
// Note: If you change the private key here, make sure to also update it in the deploy script and any other scripts that interact with the blockchain to ensure consistency.


//this is getting committed because my .env won't get staged but the DB variables here is essential for the db-init.js
//feel free to delete this after